#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--){
	    int n,x,y;
	    scanf("%d",&n);
	    scanf("%d",&x);
	    scanf("%d",&y);
	    if(n >= (x + 2*y)){
	        printf("YES");
	    }
	    else{
	        printf("NO");
	    }
	    printf("\n");
	}
	return 0;
}

