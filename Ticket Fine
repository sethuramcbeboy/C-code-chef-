#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--){
	    int x,p,q;
	    scanf("%d",&x);
	    scanf("%d",&p);
	    scanf("%d",&q);
	    if(p-q == 0){
	        printf("%d",0);
	    }
	    else{
	        printf("%d", x * (p-q));
	    }
	    printf("\n");
	}
	return 0;
}
