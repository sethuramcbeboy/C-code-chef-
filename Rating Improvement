#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--){
	    int x,y;
	    scanf("%d%d",&x,&y);
	    if(y>=x && y<=(x+200)){
	        printf("YES");
	    }
	    else{
	        printf("NO");
	    }
	    printf("\n");
	}
	return 0;
}
